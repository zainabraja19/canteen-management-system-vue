export async function ErrorHandler(status, message) {
    const errors = {}

    // if (status === 401) {
    //     await this.$store.dispatch('auth/logout');
    //     await this.$router.push(`/`);

    // } else if (status===400) {
    //     errors
    // }
    // switch (type) {
    //     case 401:
    //         // await this.$store.dispatch('auth/logout');
    //         // await this.$router.push(`/`);      
    //         break
    //     case 400:
    //         break
    //     default:
    //         break
    // }

    return errors
}